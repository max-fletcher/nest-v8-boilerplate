# File upload(MOSTLY DONE)
# Route not found response(DONE)
# Handle prisma constraint errors(e.g for unique constraint failure Prisma.PrismaClientKnownRequestError && error.code === 'P2002')(DONE)
# validation (Need to pivot to zod. Class validator doesn't enforce order.)(DONE)
# store encrypted/hashed password instead of raw password
# user registration & login route with access and refresh tokens
# Add rate limiter(DONE)
# Add cors(DONE)
# Add logger(DONE)
# Add custom exception handler(DONE)
# Relations
# Nested Relations
# Pagination for getAll calls
# DB Transactions when disjointing relational data
# Authorization and Access Control(i.e roles and permissions)
# PDF/Invoice generation
# Scheduled Tasks
# Formatted responses
# Redis
# Queue (e.g for email delivery)